---
dependency:
  name: galaxy
  requirements_file: requirements.yml

docker:
  containers:
    - name: php-fpm-ubuntu-16-04
      image: brandfrisch/ubuntu-16.04
      image_version: 'latest'
      command: /lib/systemd/systemd
      privileged: true

verifier:
  name: goss